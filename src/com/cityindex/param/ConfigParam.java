package com.cityindex.param;

public enum ConfigParam {
    BUILD_PATH,
    BUILD_NAME,
    IOS_PLATFORM_VERSION,
    IOS_APP_PACKAGE,
    IOS_DEVICE_ID,
    IOS_DEVICE,
    LOGIN,
    PASSWORD,
    TIMEOUT,
}
